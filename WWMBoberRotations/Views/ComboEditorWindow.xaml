<Window x:Class="WWMBoberRotations.Views.ComboEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Combo Editor" 
        Height="650" Width="700"
        MinHeight="500" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Edit Combo" 
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Combo Name -->
        <TextBox Grid.Row="1" 
                 materialDesign:HintAssist.Hint="Combo Name"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,15"/>

        <!-- Hotkey -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                     x:Name="HotkeyTextBox"
                     materialDesign:HintAssist.Hint="Hotkey (press a key)"
                     Text="{Binding Hotkey, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="True"
                     PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,10,0"/>
            
            <Button Grid.Column="1" 
                    Content="Clear"
                    Click="ClearHotkey_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </Grid>

        <!-- Enabled Checkbox -->
        <CheckBox Grid.Row="3" 
                  Content="Enabled"
                  IsChecked="{Binding IsEnabled}"
                  Margin="0,0,0,15"/>

        <!-- Actions List -->
        <materialDesign:Card Grid.Row="4" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Actions" 
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="15,15,15,10"/>

                <ListBox Grid.Row="1" 
                         ItemsSource="{Binding Actions}"
                         SelectedItem="{Binding SelectedAction}"
                         Margin="10">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="MinHeight" Value="32"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Margin="0" FontSize="13"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Grid Grid.Row="2" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Command="{Binding AddActionCommand}" Margin="2" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" Margin="0,0,5,0"/>
                                <TextBlock Text="Add"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding EditActionCommand}" Margin="2" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Pencil" Margin="0,0,5,0"/>
                                <TextBlock Text="Edit"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding DeleteActionCommand}" Margin="2" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Delete" Margin="0,0,5,0"/>
                                <TextBlock Text="Delete"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding MoveUpCommand}" Margin="2" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <materialDesign:PackIcon Kind="ArrowUp"/>
                        </Button>
                        
                        <Button Command="{Binding MoveDownCommand}" Margin="2" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <materialDesign:PackIcon Kind="ArrowDown"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </materialDesign:Card>

        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="SAVE" 
                    Click="Save_Click" 
                    IsDefault="True"
                    Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
            
            <Button Content="CANCEL" 
                    Click="Cancel_Click" 
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
    </Grid>
</Window>
