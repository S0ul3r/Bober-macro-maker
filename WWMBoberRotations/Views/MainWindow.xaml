<Window x:Class="WWMBoberRotations.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="WWM Bober Rotations" 
        Icon="/Assets/app_icon.png"
        Height="800" Width="1000"
        MinHeight="700" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewMouseDown="Window_PreviewMouseDown">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Start/Stop Button + Panic Button Setup -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left: Start/Stop Button (Full Width) -->
            <Button Grid.Column="0"
                    x:Name="StartStopButton"
                    Height="60"
                    Margin="0,0,10,0"
                    HorizontalAlignment="Stretch"
                    Command="{Binding ToggleSystemCommand}"
                    Style="{StaticResource PrimaryButton}">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="Play" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="START MACRO SYSTEM" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            
            <!-- Right: Panic Button Setup (Column Layout) -->
            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Right">
                <TextBlock Text="{Binding PanicButtonText}" 
                           Foreground="{DynamicResource SecondaryHueMidBrush}"
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,5"
                           TextAlignment="Right"/>
                <Button Content="Set Panic Button" 
                        Command="{Binding SetPanicButtonCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Height="36"
                        Padding="12,5"
                        FontSize="12"/>
            </StackPanel>
        </Grid>

        <!-- Main Content: Combos List + Side Panel -->
        <Grid Grid.Row="1" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left: Combos List -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Combos" 
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="15,15,15,10"/>

                    <ListBox x:Name="CombosListBox"
                             Grid.Row="1" 
                             ItemsSource="{Binding Combos}"
                             SelectedItem="{Binding SelectedCombo}"
                             Margin="10"
                             SelectionMode="Single"
                             MouseDoubleClick="CombosListBox_MouseDoubleClick">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="MinHeight" Value="44"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding IsEnabled, StringFormat='{}{0}'}" Visibility="Collapsed"/>
                                        <TextBlock FontSize="14" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Opacity" Value="1.0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                            <Setter Property="Opacity" Value="0.5"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                            <Run Text="{Binding Name, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding Hotkey, Mode=OneWay, StringFormat='[{0}]', TargetNullValue='[No Hotkey]', FallbackValue='[No Hotkey]'}" 
                                                   FontSize="11" 
                                                   Opacity="0.6"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Actions.Count, Mode=OneWay, StringFormat='{}{0} actions'}" 
                                                   FontSize="11" 
                                                   Opacity="0.6"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Grid Grid.Row="2" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                            <Button Command="{Binding NewComboCommand}" Margin="3" Style="{StaticResource PrimaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,5,0"/>
                                    <TextBlock Text="New Combo"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Command="{Binding RecordMacroCommand}" Margin="3" Style="{StaticResource PrimaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="RecordCircle" Margin="0,0,5,0"/>
                                    <TextBlock Text="Record Macro"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding EditComboCommand}" Margin="3" Style="{StaticResource SecondaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Pencil" Margin="0,0,5,0"/>
                                    <TextBlock Text="Edit"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Command="{Binding DuplicateComboCommand}" Margin="3" Style="{StaticResource SecondaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ContentCopy" Margin="0,0,5,0"/>
                                    <TextBlock Text="Duplicate"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Command="{Binding DeleteComboCommand}" Margin="3" Style="{StaticResource SecondaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Delete" Margin="0,0,5,0"/>
                                    <TextBlock Text="Delete"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:Card>
            
            <!-- Right: Side Panel with buttons and auto-save at bottom -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Top: File Operations Buttons -->
                <StackPanel Grid.Row="0" Orientation="Vertical">
                    <Button Command="{Binding SaveCombosCommand}" 
                            Margin="0,0,0,8" 
                            HorizontalAlignment="Stretch"
                            Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,5,0"/>
                            <TextBlock Text="Save"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding LoadCombosCommand}" 
                            Margin="0,0,0,8"
                            HorizontalAlignment="Stretch"
                            Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="FolderOpen" Margin="0,0,5,0"/>
                            <TextBlock Text="Load"/>
                        </StackPanel>
                    </Button>
                    
                    <Separator Margin="0,4,0,12" Opacity="0.3"/>
                    
                    <Button Command="{Binding ExportCommand}" 
                            Margin="0,0,0,8"
                            HorizontalAlignment="Stretch"
                            Style="{StaticResource SecondaryButton}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="Export" Margin="0,0,5,0"/>
                            <TextBlock Text="Export"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding ImportCommand}" 
                            Margin="0,0,0,5"
                            HorizontalAlignment="Stretch"
                            Style="{StaticResource SecondaryButton}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="Import" Margin="0,0,5,0"/>
                            <TextBlock Text="Import"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                
                <!-- Bottom: Status message + Auto-save Toggle -->
                <StackPanel Grid.Row="2" Orientation="Vertical" VerticalAlignment="Bottom">
                    <!-- Status Message -->
                    <TextBlock HorizontalAlignment="Center"
                               FontSize="11"
                               Opacity="0.8"
                               Margin="0,0,0,10"
                               Text="{Binding StatusMessage, Mode=OneWay}"
                               Visibility="{Binding StatusMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                    
                    <!-- Auto-save Toggle (single row) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="ContentSaveAll" 
                                                 Width="16" 
                                                 Height="16" 
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,5,0"
                                                 Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        <TextBlock Text="Auto-save" 
                                   VerticalAlignment="Center"
                                   FontSize="12"
                                   FontWeight="Bold"
                                   Margin="0,0,5,0"/>
                        <ToggleButton IsChecked="{Binding IsAutoSaveEnabled}"
                                      Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                      ToolTip="Auto-save every 30 seconds"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
